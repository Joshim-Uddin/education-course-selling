import React from 'react';
import logo from "./../assets/logo.svg"

const Hero = () => {
    return (
        <div>
            <div>
                <img src={logo} alt="logo image" />
            </div>
            <div></div>
        </div>
    );
};

export default Hero;